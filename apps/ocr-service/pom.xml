<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>in.noticedesk</groupId>
  <artifactId>ocr-service</artifactId>
  <version>0.1.0</version>
  <packaging>pom</packaging>
  <name>NoticeDesk OCR Service</name>
  <description>
    Self-hosted, vendor-neutral OCR for NoticeDesk. Two modules:
    ocr-core (a Spring-free, embeddable OCR engine library) and
    ocr-server (a deployable Spring Boot HTTP service around it).
  </description>

  <!--
    Spring Boot is the parent of the *server* module only. We keep it out of
    this reactor parent so ocr-core stays framework-free and can be published
    as a plain library. Versions are pinned centrally here.
  -->
  <modules>
    <module>ocr-core</module>
    <module>ocr-server</module>
  </modules>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <spring-boot.version>3.3.5</spring-boot.version>
    <pdfbox.version>3.0.3</pdfbox.version>
    <tess4j.version>5.11.0</tess4j.version>
    <junit.version>5.10.3</junit.version>
    <mockito.version>5.12.0</mockito.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.apache.pdfbox</groupId>
        <artifactId>pdfbox</artifactId>
        <version>${pdfbox.version}</version>
      </dependency>

      <dependency>
        <groupId>net.sourceforge.tess4j</groupId>
        <artifactId>tess4j</artifactId>
        <version>${tess4j.version}</version>
      </dependency>

      <!-- Internal -->
      <dependency>
        <groupId>in.noticedesk</groupId>
        <artifactId>ocr-core</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.2.5</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
